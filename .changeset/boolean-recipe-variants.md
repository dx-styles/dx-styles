---
"dx-styles": patch
---

Allow boolean selections for `recipe()` and `slotRecipe()` variant axes that declare `true` or `false`. Existing string-based selections remain supported; boolean defaults and compound variants are normalized consistently in runtime and static extraction.
