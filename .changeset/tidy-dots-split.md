---
"dx-styles": minor
---

Add `splitRecipeProps` and `splitSlotRecipeProps` for separating variant selections from component
props, plus `RecipeVariantProps` for deriving the accepted variant prop type from a recipe.
